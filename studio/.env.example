# Sanity Studio config. Copy to studio/.env — the Sanity CLI loads .env from here.
#
# projectId is hardcoded in sanity.config.ts / sanity.cli.ts (it is public, and the
# Studio only ever talks to one project). Only the dataset varies.
#
# Required — there is no default. An unset value fails with a message rather than
# guessing, because both possible guesses are wrong in one direction or the other.
# `npm run deploy` pins production itself, so this file is only about local work.
SANITY_STUDIO_DATASET=development
