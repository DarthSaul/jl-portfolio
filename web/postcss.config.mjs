/**
 * Tailwind v4, CSS-first. The entire design system lives in `src/app/globals.css` — its
 * `@theme` and `@utility` blocks are the single source for type, colour and container width.
 *
 * There is no `tailwind.config.ts` and one should not be added. See CLAUDE.md.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
